name: base
channels:
  - defaults
dependencies:
  - anaconda
  - pip
  - psycopg2 # connects to Postgres
  - pytest
  - pip:
      - neo4j
      - django
      - djangorestframework
      - django-cors-headers
      - docopt
      - ptvsd
      - pudb
      - schema # validates Python structures
